<Window x:Class="Editor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525" xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="456*" />
            <ColumnDefinition Width="47*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="248*" />
            <RowDefinition Height="63*" />
        </Grid.RowDefinitions>
        <avalonedit:TextEditor Name="TextEditor" Grid.ColumnSpan="2" />
        <Button Content="Exec" Grid.Column="1" Grid.Row="1" Name="button1" Click="button1_Click" />
        <ScrollViewer Grid.Row="1" Name="LogScrollViewer" >
            <TextBlock Name="LogTextBlock" />
        </ScrollViewer>
    </Grid>
</Window>
